# AlgoTrade Backend — Python dependencies
# Tested on Python 3.10, 3.11, 3.12 (Windows + Linux)
# Install: pip install -r requirements.txt

# ── Web framework ──────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
python-multipart==0.0.20
httpx==0.28.1

# ── Utilities ─────────────────────────────────────────────────
requests==2.32.3
python-dotenv==1.0.1

# ── Auth ──────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
bcrypt==4.2.1

# ── Data processing ───────────────────────────────────────────
pandas==2.2.3
numpy==1.26.4
pyarrow==15.0.2   # Parquet support for Bhavcopy historical cache

# ── NSE Direct API (free, no broker account needed) ───────────
nsepython==0.0.955

# ── Dhan WebSocket — optional, free with Dhan account ─────────
# Uncomment after adding DHAN_CLIENT_ID + DHAN_ACCESS_TOKEN to .env
# dhanhq==2.0.2

# ── REMOVED ───────────────────────────────────────────────────
# xlrd      — XLS feed retired, NSE Direct API used instead
# openpyxl  — not needed
