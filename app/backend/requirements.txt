# AlgoTrade Backend — Python dependencies
# Tested on Python 3.10, 3.11, 3.12 (Windows + Linux)
# Install: pip install -r requirements.txt

# ── Web framework ──────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
python-multipart==0.0.20
httpx==0.28.1

# ── Utilities ─────────────────────────────────────────────────
requests==2.32.3
python-dotenv==1.0.1

# ── Auth ──────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
pyotp==2.9.0         # TOTP auto-refresh for Dhan daily token rotation
pytz==2024.2         # IST timezone handling for 8:50 AM refresh loop

# ── Data processing ───────────────────────────────────────────
pandas==2.2.3
numpy==1.26.4
pyarrow==15.0.2

# ── NSE Direct API (free, no broker account needed) ───────────
nsepython==0.0.955

# ── Optional: Dhan WebSocket — free with Dhan account ─────────
# dhanhq==2.0.2

# ── Razorpay — payment gateway ────────────────────────────────
razorpay==1.4.2

# ── Optional: Zerodha Kite Connect ────────────────────────────
# kiteconnect==5.0.1

# ── Optional: Upstox (we use REST directly, SDK not needed) ───
# upstox-python-sdk
yfinance>=0.2.40
